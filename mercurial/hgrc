[ui]
username = Domagoj Tršan <domagoj.trsan@gmail.com>
editor = hgeditor
merge = vimdiff
verbose = true

[alias]
; Copy templates directory to /usr/local/share/mercurial and uncomment the
; following lines to enable aliases.
; Templates are taken from hgtip.com and slightly modified.
; (http://hgtip.com/tips/advanced/2010-01-15-styling-mercurials-cli/)
;nlog = log --style=/usr/local/share/mercurial/templates/map-cmdline.nlog
;slog = log --style=/usr/local/share/mercurial/templates/map-cmdline.slog
;sglog = glog --style=/usr/local/share/mercurial/templates/map-cmdline.sglog

nudge = push --rev .
show = log -pr

[auth]
example.prefix = hg.example.com
example.username = dtrsan
example.password = password
example.schema = https

[hostfingerprints]
hg.example.hr = 01:23:45:67:89:ab:cd:ef:ff:01:23:45:67:89:ab:cd:ef:ff:01:23

[extensions]
color =
churn =
graphlog =
histedit =
interhg =
largefiles =
mq =
pager =
parentrevspec =
progress =
purge =
rebase =
record =
shelve =

[color]
status.modified = cyan
status.added = green
status.removed = red
status.deleted = red underline
status.unknown = magenta
status.ignored = none

status.clean = none
status.copied = none

qseries.applied = blue underline
qseries.unapplied = none
qseries.missing = red

diff.diffline = none
diff.extended = cyan
diff.file_a = red
diff.file_b = green
diff.hunk = magenta
diff.deleted = red
diff.inserted = green
diff.changed = white
diff.trailingwhitespace = red_background

resolve.unresolved = red
resolve.resolved = green

bookmarks.current = green

branches.active = none
branches.closed = none underline
branches.current = green
branches.inactive = red

tags.normal = green
tags.local = none

[merge-tools]
vimdiff.executable = vim
vimdiff.args = -d -c "wincmd J" $output $local $other $base

[pager]
pager = LESS='FSRX' less
attend = annotate, cat, diff, export, glog, log, qdiff, slog, sglog, nlog, show
#ignore = version, help, update, serve, record

